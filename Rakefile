require 'rubygems'
require 'rake'
require 'fileutils'

task :default => [:test, :run]

desc "Play BaconBits"
task :run do
  sh "lib/ruby baconbits.rb"
end

desc "Run all tests"
task :test do
  # TODO	
end